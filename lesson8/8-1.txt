1. Для практикума из занятия 7 продумать, где можно применить замыкания.
Думаю что идеально подойдет для подсчета очков и увеличения тела змейки.


if (!("a" in window)) {
    var a = 1;
}
alert(a);

Не совсем понимаю что значит ("a" in window)

Мы проверяем на стадии подготовки a и она уже есть а значит = True.
В условии проверяем на False соответственно var a не выполнится и получим ошибку о том что значение не определено.


var b = function a(x) {
    x && a(--x);
};
alert(a);

Присваиваем b функцию.
Далее мы вызываем a но она у нас ни где не обозначена. Будет ошибка а не определено


function a(x) {
    return x * 2;
}
var a;
alert(a);

Выдаст тело функции так как в функцию ничего не передаем.


function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);

Есть функция на вход которой подаем три значения.
Далее третье значение (значение a) переопределяем arguments[2]
Соответственно на выходе получим 10


function a() {
    alert(this);
}
a.call(null);

Есть функция которая выводит сообщение текущего объекта. Текущий объект как я понимаю будет Window
Далее вызываем функцию а и передаем туда null.
Думаю что получим знаение Window.
